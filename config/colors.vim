" true colors for terminal
if (has("termguicolors"))
 set termguicolors
endif

set background=dark
" Theme
colorscheme PaperColor

let g:lightline = {
  \ 'colorscheme': 'PaperColor',
  \ }
set noshowmode
